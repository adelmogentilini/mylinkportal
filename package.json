{
  "name": "portal",
  "version": "1.0.0",
  "description": "Personal link portal with navigation by metadata",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "bot": "node --env-file-if-exists=.env telegram-bot.js"
  },
  "keywords": [
    "portal",
    "links",
    "navigation"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "node-telegram-bot-api": "^1.2.0"
  }
}
